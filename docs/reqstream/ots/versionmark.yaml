---
# VersionMark OTS requirements

sections:
  - title: OTS Software Requirements
    sections:
      - title: VersionMark Requirements
        requirements:
          - id: DictionaryMark-OTS-VersionMark
            title: VersionMark shall publish captured tool-version information.
            justification: |
              DemaConsulting.VersionMark reads version metadata for each dotnet tool used in the
              pipeline and writes a versions markdown document included in the release artifacts.
              It runs in the same CI pipeline that produces the TRX test results, so a successful
              pipeline run is evidence that VersionMark executed without error.
            tags: [ots]
            tests:
              - VersionMark_CapturesVersions
              - VersionMark_GeneratesMarkdownReport
          - id: DictionaryMark-OTS-VersionMark-Lint
            title: VersionMark shall validate tool-version configuration files.
            justification: |
              DemaConsulting.VersionMark provides a lint mode that validates .versionmark.yaml
              configuration files, reporting structural and semantic errors before capture or
              publish operations are attempted. A successful lint pass confirms that the
              configuration is well-formed; a lint failure correctly identifies configuration
              problems.
            tags: [ots]
            tests:
              - VersionMark_LintPassesForValidConfig
              - VersionMark_LintReportsErrorsForInvalidConfig
